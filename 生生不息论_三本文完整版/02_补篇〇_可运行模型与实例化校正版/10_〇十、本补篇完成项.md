## 〇十、本补篇完成项

| v13.2 待办 | 本补篇处理 |
|---|---|
| $\Omega$ 至少三种模型 | 已给 $\Omega_{\min}$、$\Omega_{\mathrm{geo}}$、$\Omega_{\mathrm{guard}}$ |
| $\Omega_B$ 至少三种模型 | 已给 $\Omega_{B,\min}$、$\Omega_{B,\mathrm{nash}}$、$\Omega_{B,q}$ |
| $\Pi_{\mathrm{开}}$ 场景化判准 | 已给投影包、状态判定、行动后场投影 |
| 审校数据结构 | 已给事件表、迹表、回应表、模型互证表、度期后果表 |
| 阈值设置 | 已给双阈值、三档默认、阈值生成流程 |
| 案例跑通 | 已给推荐系统案例，从 $\Gamma$、$I_F$、$\oplus_F$ 到正邪仁道 |

仍不可取消的限制：

1. 具体领域仍须填真实数据；
2. 阈值仍须由锚例、损害等级与审校史校准；
3. “真道”仍需跨相关度期持续通过，不可由单次案例直接封称；
4. 模型冲突时仍须输出 $\mathsf{U}$ 或提高审校等级。

> **至此，本合订版由“可查”推进为“可跑”；但“可跑”不等于“已经验终证”。**

---

