## 〇八、判定伪代码

```text
run(Gamma, B, s, omega, candidates, H, Theta, modelSet):
  I = I_F(Gamma; s, omega, H)
  if I is unknown:
    return U

  basePi = Pi_open(Gamma; s, omega, H, Theta, modelSet.Omega)

  for each i in candidates:
    if i not in I:
      label[i] = illegal
      continue

    Gamma_i = Gamma op_F i
    pi[i] = Pi_open(Gamma_i; s, omega, H, Theta, modelSet.Omega)
    co[i] = Omega_B(B, Gamma_i; s, omega, modelSet.Omega_B)
    audit[i] = audit_score(i, Gamma, Gamma_i, H)

    if audit[i] below audit threshold:
      label[i] = U
    else if capture_or_fake_open_or_frozen(Gamma, i, B):
      label[i] = evil_action
    else if protects_open_or_needed_protective_closure(Gamma, i, B):
      label[i] = right_action
    else:
      label[i] = U

  choose i only if label[i] is right_action
  derive yi, shan, ren, dao through the protocols in Volume XIV
```

