/-
# OXNotation — `OX["..."]` 8-char string literal placeholder

The `OX["..."]` 8-character ASCII literal for `R 8` cells used to live
here, layered on the Yi-flavored Hexagram × Shi decomposition from
`Foundation/Bagua/R8.lean`.

Per the v0.6 Atlas-separation doctrine, an `R 8 = Fin 8 → Bool` string
literal belongs adjacent to the language-independent R-Family core
(`Foundation/R/`).  Until that lives in a clean Atlas overlay, this
file is a placeholder and exposes no macro.
-/
import SSBX.Foundation.R.Basic

namespace SSBX.Foundation.Notation.OXNotation

end SSBX.Foundation.Notation.OXNotation
