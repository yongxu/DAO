/-
# Foundation.Hierarchy.ZoneClassifier — 顶层入口

任意位 oxox 的全轴判定 + 道/理/可理/不可理 子集化。

详见各子模块的 docstring。
-/

import SSBX.Foundation.Hierarchy.ZoneClassifier.Types
import SSBX.Foundation.Hierarchy.ZoneClassifier.WallCitations
import SSBX.Foundation.Hierarchy.ZoneClassifier.Operators
import SSBX.Foundation.Hierarchy.ZoneClassifier.Axes
import SSBX.Foundation.Hierarchy.ZoneClassifier.Classify
import SSBX.Foundation.Hierarchy.ZoneClassifier.Subsets
import SSBX.Foundation.Hierarchy.ZoneClassifier.Interaction
import SSBX.Foundation.Hierarchy.ZoneClassifier.Examples
-- Lift.lean 已删除（见 plan 第二轮）；R_n → R_{2n} 之 zone 提升需 RetractTower 派生，留待后续
