/-
Wenyan operator catalogue generated from `wenyan-operators.md` headings.

This module is the compatibility entry point.  The large catalogue is split
under `SSBX.Text.WenyanOperators.*` so Lake can build the generated tables in
smaller pieces.
-/
import SSBX.Text.WenyanOperators.Audit
