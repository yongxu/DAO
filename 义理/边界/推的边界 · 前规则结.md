# 推的边界 · 前规则结

**前置**：[边界文档索引](README.md) · [边界总图谱 · 知感行证名量测推](%E8%BE%B9%E7%95%8C%E6%80%BB%E5%9B%BE%E8%B0%B1%20%C2%B7%20%E7%9F%A5%E6%84%9F%E8%A1%8C%E8%AF%81%E5%90%8D%E9%87%8F%E6%B5%8B%E6%8E%A8.md) · [形式逻辑 · 从元到推](../形式逻辑%20·%20从元到推.md) · [证的边界 · 命题系统证书](%E8%AF%81%E7%9A%84%E8%BE%B9%E7%95%8C%20%C2%B7%20%E5%91%BD%E9%A2%98%E7%B3%BB%E7%BB%9F%E8%AF%81%E4%B9%A6.md) · [统计 · 从元到测](../统计%20·%20从元到测.md) · [测的边界 · 观验读](%E6%B5%8B%E7%9A%84%E8%BE%B9%E7%95%8C%20%C2%B7%20%E8%A7%82%E9%AA%8C%E8%AF%BB.md) · [四界不混同 · 测量推感](%E5%9B%9B%E7%95%8C%E4%B8%8D%E6%B7%B7%E5%90%8C%20%C2%B7%20%E6%B5%8B%E9%87%8F%E6%8E%A8%E6%84%9F.md)

**Lean 锚点**：

| 层 | 文件 | 内容 |
|---|---|---|
| 推边界 | `Foundation/Modern/TuiBoundary.lean` | `TuiAxis = premise | rule | derivation | conclusion | warrant` |
| 前提边界 | `Foundation/Modern/TuiBoundary.lean` | `premise_boundary_complete` |
| 规则边界 | `Foundation/Modern/TuiBoundary.lean` | `rule_boundary_complete` |
| 推导边界 | `Foundation/Modern/TuiBoundary.lean` | `derivation_boundary_complete` |
| 结论边界 | `Foundation/Modern/TuiBoundary.lean` | `conclusion_boundary_complete` |
| 担保边界 | `Foundation/Modern/TuiBoundary.lean` | `warrant_boundary_complete` |
| 测与推 | `Foundation/Modern/CeBoundary.lean` | `measurement_record_ne_inference_claim` |
| 自然演绎锚点 | `Foundation/Modern/NaturalDeduction.lean` | `imp_elim`，由 `modus_ponens_anchor` 接入 |

> 本文完成“推”的 typed skeleton：推不是测、不是量、不是感。完整的推，至少要有前提、规则、推导、结论、担保五个接口；测量记录可以供给前提，但不等于推断命题。

---

## 〇一 · 完成度审计

本轮“推”类证明完成到五条轴：

| 轴 | 内侧 | 外侧 | Lean 状态 | theorem |
|---|---|---|---|---|
| 前提 | 前提可用 | 前提缺失 | 已证 typed skeleton | `premise_boundary_complete` |
| 规则 | 规则已指定 | 规则未指定 | 已证 typed skeleton | `rule_boundary_complete` |
| 推导 | 已完成推导 | 未完成推导 | 已证 typed skeleton | `derivation_boundary_complete` |
| 结论 | 已得结论 | 未得结论 | 已证 typed skeleton | `conclusion_boundary_complete` |
| 担保 | 有担保 / 可辩护 | 无担保 | 已证 typed skeleton | `warrant_boundary_complete` |

本轮同时完成：

| 交接 | Lean 状态 | theorem |
|---|---|---|
| 完整推理流程产出 inference claim | 已证 | `complete_tui_yields_inference_claim` |
| 缺项流程不产出 inference claim | 已证 | `incomplete_tui_not_inference_claim` |
| 推断命题不同于测量记录 | 已证 | `tui_claim_ne_measurement_record` |
| Modus ponens 最小锚点 | 已证 | `modus_ponens_anchor` |

未纳入本轮：

| 项 | 状态 | 说明 |
|---|---|---|
| 完整证明论 | 未纳入本轮 | 不证明所有系统的可靠性、完备性或独立性 |
| 统计推断的全部细节 | 未纳入本轮 | 不展开估计量、p-value、后验一致性 |
| 因果推断 | 未纳入本轮 | do-calculus / 反事实另开 |

所以这里的“完成”有精确范围：**前提、规则、推导、结论、担保五轴已闭合；推与测的产物区分已证明；完整证明论、统计推断、因果推断不由本文替代。**

---

## 〇 · 边界声明

| 命题 | 本文处理方式 | 不声称 |
|---|---|---|
| 推有五条轴 | Lean 枚举并证明两两不同 | 不声称所有推理理论只有五步 |
| 每条轴内外穷尽互斥 | typed skeleton | 不判定具体论证是否 sound |
| 完整推理产出 inference claim | `CompleteTui → inferenceClaim` | 不说 inference claim 一定为真 |
| 测量记录不是推断命题 | `measurementRecord ≠ inferenceClaim` | 不否认记录可作为前提 |

核心区分：

```text
推 ≠ 测
推 ≠ 量
推 ≠ 感
```

推可以使用测量记录，可以包含数量，可以从感受报告出发；但推的产物是结论 / claim，而不是记录、数值或感受本身。

---

## 一 · 五条推轴

Lean 中：

```lean
inductive TuiAxis : Type
  | premise
  | rule
  | derivation
  | conclusion
  | warrant
```

并证明五轴两两不同：

```lean
theorem tui_axes_pairwise_distinct :
    TuiAxis.premise ≠ TuiAxis.rule
    ∧ TuiAxis.premise ≠ TuiAxis.derivation
    ∧ TuiAxis.premise ≠ TuiAxis.conclusion
    ∧ TuiAxis.premise ≠ TuiAxis.warrant
    ∧ TuiAxis.rule ≠ TuiAxis.derivation
    ∧ TuiAxis.rule ≠ TuiAxis.conclusion
    ∧ TuiAxis.rule ≠ TuiAxis.warrant
    ∧ TuiAxis.derivation ≠ TuiAxis.conclusion
    ∧ TuiAxis.derivation ≠ TuiAxis.warrant
    ∧ TuiAxis.conclusion ≠ TuiAxis.warrant
```

读法：

| 轴 | 问题 |
|---|---|
| 前提 | 有什么可以作为输入 |
| 规则 | 用什么规则从前提走到结论 |
| 推导 | 推导过程是否完成 |
| 结论 | 是否得到 claim |
| 担保 | 结论是否可辩护 / 有依据 |

---

## 二 · 完整推理流程

Lean 中：

```lean
structure TuiSkeleton where
  premise : PremiseRegion
  rule : RuleRegion
  derivation : DerivationRegion
  conclusion : ConclusionRegion
  warrant : WarrantRegion
```

完整推理定义为：

```lean
def CompleteTui (t : TuiSkeleton) : Prop :=
  PremiseAvailable t.premise
    ∧ RuleSpecified t.rule
    ∧ Derived t.derivation
    ∧ Concluded t.conclusion
    ∧ Warranted t.warrant
```

它映射到 `CeBoundary` 中区分出来的推断产物：

```lean
def tuiAsCeProduct (t : TuiSkeleton) : CeProduct :=
  match t.premise, t.rule, t.derivation, t.conclusion, t.warrant with
  | .available, .specified, .derived, .concluded, .warranted => .inferenceClaim
  | _, _, _, _, _ => .measurementRecord
```

主证明：

```lean
theorem complete_tui_yields_inference_claim (t : TuiSkeleton) :
    CompleteTui t → tuiAsCeProduct t = .inferenceClaim

theorem incomplete_tui_not_inference_claim (t : TuiSkeleton) (h : ¬ CompleteTui t) :
    tuiAsCeProduct t = .measurementRecord
```

这里的 `measurementRecord` 是“非完整推断产物”的保守外侧标签；它强调缺项时不能冒充 inference claim。

---

## 三 · 最小推理锚点：故 / Modus Ponens

自然演绎中最小的“推”是 modus ponens：

```lean
theorem modus_ponens_anchor {P Q : Prop} (hpq : P → Q) (hp : P) : Q :=
  SSBX.Foundation.Modern.NaturalDeduction.imp_elim hpq hp
```

读法：

```text
若 P 则 Q；
P；
故 Q。
```

这不是完整证明论，只是把“推”的最小规则锚到已有 `NaturalDeduction.lean`。

---

## 四 · 测量记录不是推断命题

`CeBoundary.lean` 已经证明：

```lean
theorem measurement_record_ne_inference_claim :
    CeProduct.measurementRecord ≠ CeProduct.inferenceClaim
```

本文从推的方向重述：

```lean
theorem tui_claim_ne_measurement_record :
    CeProduct.inferenceClaim ≠ CeProduct.measurementRecord
```

读法：

| 层 | 产物 |
|---|---|
| 测 | record |
| 推 | claim |

record 可以成为 premise，但 record 自己不是 conclusion。

---

## 五 · 主定理

```lean
theorem tui_boundary_summary :
    ...
    ∧ (∀ t : TuiSkeleton, CompleteTui t → tuiAsCeProduct t = .inferenceClaim)
    ∧ (∀ t : TuiSkeleton, ¬ CompleteTui t → tuiAsCeProduct t = .measurementRecord)
    ∧ CeProduct.inferenceClaim ≠ CeProduct.measurementRecord
```

总读法：

1. 五条“推”轴不同；
2. 五条轴各自内外穷尽且互斥；
3. 完整推理流程产出 inference claim；
4. 缺任一项的流程不能产出 inference claim；
5. 推断命题不同于测量记录。

---

## 六 · 结论

```text
推
├─ 前提：输入可用
├─ 规则：推理规则已指定
├─ 推导：过程已完成
├─ 结论：claim 已得出
└─ 担保：结论可辩护
```

推可以接收测量、数量、感受作为材料；但推的完成条件是规则化导出结论。

验证命令：

```bash
lake build SSBX.Foundation.Modern.TuiBoundary
```
