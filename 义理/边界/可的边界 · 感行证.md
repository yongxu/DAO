# 可的边界 · 感行证

**前置**：[边界文档索引](README.md) · [边界总图谱 · 知感行证名量测推](%E8%BE%B9%E7%95%8C%E6%80%BB%E5%9B%BE%E8%B0%B1%20%C2%B7%20%E7%9F%A5%E6%84%9F%E8%A1%8C%E8%AF%81%E5%90%8D%E9%87%8F%E6%B5%8B%E6%8E%A8.md) · [可知边界 · 语言与理](%E5%8F%AF%E7%9F%A5%E8%BE%B9%E7%95%8C%20%C2%B7%20%E8%AF%AD%E8%A8%80%E4%B8%8E%E7%90%86.md) · [感的边界 · 受觉现](%E6%84%9F%E7%9A%84%E8%BE%B9%E7%95%8C%20%C2%B7%20%E5%8F%97%E8%A7%89%E7%8E%B0.md) · [行的边界 · 能境作效](%E8%A1%8C%E7%9A%84%E8%BE%B9%E7%95%8C%20%C2%B7%20%E8%83%BD%E5%A2%83%E4%BD%9C%E6%95%88.md) · [证的边界 · 命题系统证书](%E8%AF%81%E7%9A%84%E8%BE%B9%E7%95%8C%20%C2%B7%20%E5%91%BD%E9%A2%98%E7%B3%BB%E7%BB%9F%E8%AF%81%E4%B9%A6.md) · [名的边界 · 指称定名](%E5%90%8D%E7%9A%84%E8%BE%B9%E7%95%8C%20%C2%B7%20%E6%8C%87%E7%A7%B0%E5%AE%9A%E5%90%8D.md) · [量的边界 · 数测度](%E9%87%8F%E7%9A%84%E8%BE%B9%E7%95%8C%20%C2%B7%20%E6%95%B0%E6%B5%8B%E5%BA%A6.md) · [统一边界 · 从理到文](%E7%BB%9F%E4%B8%80%E8%BE%B9%E7%95%8C%20%C2%B7%20%E4%BB%8E%E7%90%86%E5%88%B0%E6%96%87.md) · [心智 · 从元到识](../心智%20·%20从元到识.md) · [动力 · 从元到行](../动力%20·%20从元到行.md) · [形式逻辑 · 从元到推](../形式逻辑%20·%20从元到推.md)

**Lean 锚点**：

| 层 | 文件 | 内容 |
|---|---|---|
| 可知边界 | `Foundation/Modern/KnowableBoundary.lean` | 可知 = 可说 = 可描述之理 |
| 可感边界 | `Foundation/Modern/KeBoundary.lean` | `SensibilityRegion = feelable | unfeelable` |
| 感流程边界 | `Foundation/Modern/GanBoundary.lean` | `GanAxis = exposure | reception | affection | phenomenality | report` |
| 可行边界 | `Foundation/Modern/KeBoundary.lean` | `ActionabilityRegion = actionable | unactionable` |
| 行流程边界 | `Foundation/Modern/XingBoundary.lean` | `XingAxis = agent | capacity | context | operation | effect` |
| 可证边界 | `Foundation/Modern/KeBoundary.lean` | `ProofRegion = provable | unprovable`，仅作 typed skeleton |
| 证流程边界 | `Foundation/Modern/ZhengBoundary.lean` | `ZhengAxis = proposition | system | derivation | checker | certificate` |
| 感行证承接 | `Foundation/Modern/KeBoundary.lean` | `feel_act_proof_boundaries_handled_here` |
| 可测交接 | `Foundation/Modern/KeBoundary.lean` / `Foundation/Modern/LiangBoundary.lean` | `measurable_boundaries_not_handled_in_ke` / `measurable_deferred_boundaries_handled_in_liang` |
| 可名交接 | `Foundation/Modern/KeBoundary.lean` / `Foundation/Modern/MingBoundary.lean` | `name_boundaries_not_handled_in_ke` / `name_deferred_boundaries_handled_in_ming` |

> 本文接上前文留出的三组边界：
>
> 1. 可感 / 不可感；
> 2. 可行 / 不可行；
> 3. 可证 / 不可证。
>
> 结论：它们都属于“可”的边界，但不应直接混同为“可知”。可知处理语言与理；可感处理现象是否进入感受接口；可行处理是否进入实践接口；可证处理是否进入当前 proof interface。

---

## 〇一 · 完成度审计

本轮“可”类证明已完成到四条轴：

| 轴 | 内侧 | 外侧 | Lean 状态 | 边界 |
|---|---|---|---|---|
| 可知 | 可说 / 可描述为理 | 不可作为理说出 | 已证 | `KnowableBoundary.lean` |
| 可感 | 可进入感受接口 | 不可进入感受接口 | 已证 typed skeleton；流程细化见《感的边界 · 受觉现》 | `sensibility_boundary_complete` |
| 可行 | 可进入行动接口 | 不可进入行动接口 | 已证 typed skeleton；流程细化见《行的边界 · 能境作效》 | `actionability_boundary_complete` |
| 可证 | 可进入当前 proof interface | 不可进入当前 proof interface | 已证 typed skeleton；流程细化见《证的边界 · 命题系统证书》 | `proof_boundary_complete` |

不由本文直接展开、但已有承接的“可”：

| 轴 | 状态 | 说明 |
|---|---|---|
| 可测 / 不可测 | 已承接，未在本文展开 | 见《量的边界 · 数测度》，接统计与量子测量接口 |
| 可名 / 不可名 | 已承接，未在本文展开 | 见《名的边界 · 指称定名》，接指称、符号、约定、域、消歧接口 |

所以这里的“完成”有精确范围：**本文闭合可知、可感、可行、可证四轴；可测 / 不可测不纳入本文展开，已由“量”的边界承接；可名 / 不可名不纳入本文展开，已由“名”的边界承接。**

---

## 〇 · 边界声明

本文仍然很窄：

| 边界 | 本文是否处理 | 处理方式 |
|---|---|---|
| 可感 / 不可感 | 是 | typed skeleton：穷尽且互斥 |
| 可行 / 不可行 | 是 | typed skeleton：穷尽且互斥 |
| 可证 / 不可证 | 是，但有限 | 只说明 proof interface 内外，不展开证明论 |
| 可知 / 不可知 | 引用前文 | 不重写 |
| 可测 / 不可测 | 不在本文处理 | 已由《量的边界 · 数测度》承接 |
| 可名 / 不可名 | 不在本文处理 | 已由《名的边界 · 指称定名》承接；不并入本文的感行证 claim |

本文的任务不是一次性完成所有“可”，而是把前文明确留出的感、行、证三项接上；量与名由相邻边界文件承接。

---

## 一 · “可”的轴

“可”不是一个单轴词。

| 轴 | 问题 | 典型误读 |
|---|---|---|
| 可知 | 能否作为可描述之理被知道 | 把知道等同于感到 |
| 可感 | 能否进入感受 / 现象接口 | 把感到等同于证明 |
| 可行 | 能否进入行动 / 实践接口 | 把可说等同于可做 |
| 可证 | 能否进入证明接口 | 把不可证等同于无意义 |

Lean 中先给轴：

```lean
inductive KeAxis : Type
  | know
  | sense
  | act
  | prove
```

并证明这些轴不能直接塌缩：

```lean
theorem ke_axes_pairwise_distinct :
    KeAxis.know ≠ KeAxis.sense
    ∧ KeAxis.know ≠ KeAxis.act
    ∧ KeAxis.know ≠ KeAxis.prove
    ∧ KeAxis.sense ≠ KeAxis.act
    ∧ KeAxis.sense ≠ KeAxis.prove
    ∧ KeAxis.act ≠ KeAxis.prove
```

这一步很关键：可感、可行、可证都和可知有关，但不是可知的别名。

---

## 二 · 可感 / 不可感

可感边界问的是：

```
某物是否进入感受 / 现象接口？
```

本文只给“可感 / 不可感”的边界；完整感受流程由 [感的边界 · 受觉现](%E6%84%9F%E7%9A%84%E8%BE%B9%E7%95%8C%20%C2%B7%20%E5%8F%97%E8%A7%89%E7%8E%B0.md) 细化为触、受、感、现、报五轴。

它不是问“能不能证明”，也不是问“能不能说清”。

Lean 中：

```lean
inductive SensibilityRegion : Type
  | feelable
  | unfeelable
```

定义：

```lean
def Feelable (r : SensibilityRegion) : Prop :=
  r = .feelable

def Unfeelable (r : SensibilityRegion) : Prop :=
  r = .unfeelable
```

证明：

```lean
theorem sensibility_boundary_complete :
    (∀ r : SensibilityRegion, Feelable r ∨ Unfeelable r)
    ∧ (∀ r : SensibilityRegion, Feelable r → ¬ Unfeelable r)
    ∧ (∀ r : SensibilityRegion, Unfeelable r → ¬ Feelable r)
```

读法：

| 命题 | 含义 |
|---|---|
| 穷尽 | 在当前感受接口中，只分可感 / 不可感 |
| 互斥 | 同一对象不能在同一接口下既可感又不可感 |

这只是感受边界的 typed skeleton。它不说明感受内容是什么，不说明 qualia，也不说明主体间验证。

---

## 三 · 可行 / 不可行

可行边界问的是：

```
某物是否能进入行动 / 实践接口？
```

可行不等于可知。

有些东西可知但不可行：

```
我知道某个天体轨道，但不能改变它。
```

有些东西可行但未必已经被完整说清：

```
身体技能、习惯动作、现场判断。
```

Lean 中：

```lean
inductive ActionabilityRegion : Type
  | actionable
  | unactionable
```

证明：

```lean
theorem actionability_boundary_complete :
    (∀ r : ActionabilityRegion, Actionable r ∨ Unactionable r)
    ∧ (∀ r : ActionabilityRegion, Actionable r → ¬ Unactionable r)
    ∧ (∀ r : ActionabilityRegion, Unactionable r → ¬ Actionable r)
```

读法：

| 命题 | 含义 |
|---|---|
| 穷尽 | 在当前行动接口中，只分可行 / 不可行 |
| 互斥 | 同一对象不能在同一接口下既可行又不可行 |

这同样不展开行动理论；只先把边界形式安放。

---

## 四 · 可证 / 不可证

可证边界与可知最接近，但仍不能混同。

可知边界说：

```
是否能作为可描述之理进入语言？
```

可证边界说：

```
是否能在当前 proof interface 中给出证明？
```

一个命题可以有意义、可说、可知，但在某个形式系统中不可证。

因此本文只做 typed skeleton：

```lean
inductive ProofRegion : Type
  | provable
  | unprovable
```

证明：

```lean
theorem proof_boundary_complete :
    (∀ r : ProofRegion, Provable r ∨ Unprovable r)
    ∧ (∀ r : ProofRegion, Provable r → ¬ Unprovable r)
    ∧ (∀ r : ProofRegion, Unprovable r → ¬ Provable r)
```

同时说明：这组 proof-boundary 的区分本身仍然可说：

```lean
def proofRegionAsKnowable (_r : ProofRegion) : KnowableRegion :=
  .withinDescribableLi

theorem proof_boundary_is_sayable_skeleton (r : ProofRegion) :
    Sayable (proofRegionAsKnowable r)
```

这不是说所有不可证命题都已被解决。恰恰相反：本文只证明“可证 / 不可证”这个边界可以被语言表达；不证明具体命题是否可证。

---

## 五 · 三组边界的关系

| 边界 | 内侧 | 外侧 | 与可知关系 |
|---|---|---|---|
| 可知 | 可描述之理 | 不可作为理说出 | 主轴 |
| 可感 | 可进入感受接口 | 不可进入感受接口 | 不等于可知 |
| 可行 | 可进入行动接口 | 不可进入行动接口 | 不等于可知 |
| 可证 | 可在 proof interface 内证明 | 不可在该 interface 内证明 | 用可知语言表达，但不等于可知 |

所以：

```
可知 ≠ 可感 ≠ 可行 ≠ 可证
```

更准确：

```
它们是四个不同接口上的“可”。
```

---

## 六 · 承接前文留项

前文 [可知边界 · 语言与理](%E5%8F%AF%E7%9F%A5%E8%BE%B9%E7%95%8C%20%C2%B7%20%E8%AF%AD%E8%A8%80%E4%B8%8E%E7%90%86.md) 明确说：

| 边界 | 当时处理吗 |
|---|---|
| 可感 / 不可感 | 不在可知文内处理 |
| 可行 / 不可行 | 不在可知文内处理 |
| 可证 / 不可证 | 只指出相关，不展开 proof skeleton |
| 可测 / 不可测 | 不在可知文内处理 |
| 可名 / 不可名 | 不在可知文内处理 |

本文接住感、行、证三组留项；可测 / 不可测由 [量的边界 · 数测度](%E9%87%8F%E7%9A%84%E8%BE%B9%E7%95%8C%20%C2%B7%20%E6%95%B0%E6%B5%8B%E5%BA%A6.md) 接住；可名 / 不可名由 [名的边界 · 指称定名](%E5%90%8D%E7%9A%84%E8%BE%B9%E7%95%8C%20%C2%B7%20%E6%8C%87%E7%A7%B0%E5%AE%9A%E5%90%8D.md) 接住。

Lean 中保留下列交接证明：

```lean
theorem feel_act_proof_boundaries_handled_here :
    handledHere .feelable = false ∧ handledInKeBoundary .feelable = true
    ∧ handledHere .unfeelable = false ∧ handledInKeBoundary .unfeelable = true
    ∧ handledHere .actionable = false ∧ handledInKeBoundary .actionable = true
    ∧ handledHere .unactionable = false ∧ handledInKeBoundary .unactionable = true
    ∧ handledHere .provable = false ∧ handledInKeBoundary .provable = true
    ∧ handledHere .unprovable = false ∧ handledInKeBoundary .unprovable = true

theorem measurable_boundaries_not_handled_in_ke :
    handledHere .measurable = false ∧ handledInKeBoundary .measurable = false
    ∧ handledHere .unmeasurable = false ∧ handledInKeBoundary .unmeasurable = false

theorem name_boundaries_not_handled_in_ke :
    handledHere .nameable = false ∧ handledInKeBoundary .nameable = false
    ∧ handledHere .unnameable = false ∧ handledInKeBoundary .unnameable = false
```

读法：

```
KnowableBoundary 不处理这些非可知轴；
KeBoundary 承接可感 / 可行 / 可证；
KeBoundary 不承接可测 / 不可测，也不承接可名 / 不可名。
```

---

## 七 · 主定理

```lean
theorem ke_boundary_summary :
    (KeAxis.know ≠ KeAxis.sense
      ∧ KeAxis.know ≠ KeAxis.act
      ∧ KeAxis.know ≠ KeAxis.prove
      ∧ KeAxis.sense ≠ KeAxis.act
      ∧ KeAxis.sense ≠ KeAxis.prove
      ∧ KeAxis.act ≠ KeAxis.prove)
    ∧ (∀ b : KeBoundary, b.side = .inside ∨ b.side = .outside)
    ∧ (∀ r : SensibilityRegion, Feelable r ∨ Unfeelable r)
    ∧ (∀ r : SensibilityRegion, Feelable r → ¬ Unfeelable r)
    ∧ (∀ r : SensibilityRegion, Unfeelable r → ¬ Feelable r)
    ∧ (∀ r : ActionabilityRegion, Actionable r ∨ Unactionable r)
    ∧ (∀ r : ActionabilityRegion, Actionable r → ¬ Unactionable r)
    ∧ (∀ r : ActionabilityRegion, Unactionable r → ¬ Actionable r)
    ∧ (∀ r : ProofRegion, Provable r ∨ Unprovable r)
    ∧ (∀ r : ProofRegion, Provable r → ¬ Unprovable r)
    ∧ (∀ r : ProofRegion, Unprovable r → ¬ Provable r)
    ∧ (∀ r : ProofRegion, Sayable (proofRegionAsKnowable r))
    ∧ (handledHere .feelable = false ∧ handledInKeBoundary .feelable = true
      ∧ handledHere .unfeelable = false ∧ handledInKeBoundary .unfeelable = true
      ∧ handledHere .actionable = false ∧ handledInKeBoundary .actionable = true
      ∧ handledHere .unactionable = false ∧ handledInKeBoundary .unactionable = true
      ∧ handledHere .provable = false ∧ handledInKeBoundary .provable = true
      ∧ handledHere .unprovable = false ∧ handledInKeBoundary .unprovable = true)
    ∧ (handledHere .measurable = false ∧ handledInKeBoundary .measurable = false
      ∧ handledHere .unmeasurable = false ∧ handledInKeBoundary .unmeasurable = false)
    ∧ (handledHere .nameable = false ∧ handledInKeBoundary .nameable = false
      ∧ handledHere .unnameable = false ∧ handledInKeBoundary .unnameable = false)
```

这就是本文完成的最小闭包。

---

## 八 · 结论

前文已经给出：

```
可知 = 可说 = 可描述之理
```

本文补齐：

```
可感 / 不可感
可行 / 不可行
可证 / 不可证
```

但补齐不等于混同；可测 / 不可测也不强行塞入感、行、证，而是交给“量”的边界。
可名 / 不可名同理，不强行塞入可知或感行证，而是交给“名”的边界。

最终结构是：

```
可
├─ 可知：语言可描述之理
├─ 可感：能否进入感受接口
├─ 可行：能否进入行动接口
├─ 可证：能否进入当前 proof interface
├─ 可测：见《量的边界 · 数测度》
└─ 可名：见《名的边界 · 指称定名》
```

其中可证最接近可知，但也只是“用可知语言表达的证明边界”，不是可知本身。

---

## 九 · Lean 证明入口

新增文件：

```text
formal/SSBX/Foundation/Modern/KeBoundary.lean
```

核心 theorem：

| theorem | 内容 |
|---|---|
| `ke_axes_pairwise_distinct` | 可知 / 可感 / 可行 / 可证 四轴两两不同 |
| `boundary_side_exhaustive` / `ke_boundary_side_exhaustive` | 任一边界侧都落在 inside / outside |
| `sensibility_exhaustive` | 可感 / 不可感 穷尽 |
| `feelable_not_unfeelable` | 可感与不可感互斥 |
| `sensibility_boundary_complete` | 可感边界的穷尽 + 双向互斥总表 |
| `actionability_exhaustive` | 可行 / 不可行 穷尽 |
| `actionable_not_unactionable` | 可行与不可行互斥 |
| `actionability_boundary_complete` | 可行边界的穷尽 + 双向互斥总表 |
| `proof_region_exhaustive` | 可证 / 不可证 typed skeleton 穷尽 |
| `provable_not_unprovable` | 可证与不可证互斥 |
| `proof_boundary_complete` | 可证边界的穷尽 + 双向互斥总表 |
| `proof_boundary_is_sayable_skeleton` | 证明边界本身仍可用可知语言表达 |
| `feel_act_proof_boundaries_handled_here` | 承接可感 / 可行 / 可证留项 |
| `measurable_boundaries_not_handled_in_ke` | 明确可测 / 不可测不由本文处理 |
| `name_boundaries_not_handled_in_ke` | 明确可名 / 不可名不由本文处理 |
| `ke_boundary_summary` | 本文总摘要 |

验证命令：

```bash
lake build SSBX.Foundation.Modern.KeBoundary
```
