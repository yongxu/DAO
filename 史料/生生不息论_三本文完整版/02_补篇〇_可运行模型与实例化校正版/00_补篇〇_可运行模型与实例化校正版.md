# 补篇〇 · 可运行模型与实例化校正版

本补篇回应 v13.2 自检表中“下一版应做之事”。其目标不是把所有经验模型定为唯一真式，而是给出一套可实际运行、可替换、可审校的初始协议：

1. 给 $\Omega$ 与 $\Omega_B$ 三种可选模型；
2. 给 $\Pi_{\mathrm{开}}$ 的场景化投影定义；
3. 给审校数据结构；
4. 给阈值设定与三值缓冲法；
5. 给一个从 $\Gamma$、$I_F$、$\oplus_F$ 到正邪仁道的最小跑通案例。

总原则：

> **模型可多，接口须一；阈值可境性，校验须可查；输出可待校，不得伪作已真。**

