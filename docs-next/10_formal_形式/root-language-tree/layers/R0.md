# R0 太极 / Unit — root language entries

> 状态：generated draft v0.1 (2026-05-11)
> 角色：1023 审名包中的 interface 分层审阅页。每个 bitstring 同时给出 cell / operator 两种读法。没有稳定字对应时，文言列回退为 ox。
> 本层计数：`2 × 1 = 2` entries。

| id | ox | role | 文言候选 | 中文候选 | English | formal logic | scheme | 备选与理由 |
|---|---|---|---|---|---|---|---|---|
| R0-ε-cell | ε | cell | 太极 | 一元根 | unit root / Taiji | `() : R0` | cell root | canonical 太极/一元；单字 alt: 极；interface table keeps R0 cell separate from R0 operator |
| R0-ε-operator | ε | operator | 恒 | 恒等算子 | identity operator | `id : R0 -> R0` | operator-as-mask | R0 operator role in the 1022 interface subtotal；ontology tree may not duplicate R0 |

## 审阅说明

- 本页是 `1023` 审名包中的 R0 interface subtotal，因此 R0 也列 cell/operator 两项；若用 ontology 口径，R0 不重复，合计为 `1021`。
