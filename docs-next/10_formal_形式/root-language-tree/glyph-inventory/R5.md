# R5 內 — 64 interface glyph inventory

> 状态：审名 v0.1 (2026-05-13)。
> 口径：逐行列出 R5 的 32 个 cell 实字 alias 与 32 个 operator 字组。
> 判断：R5 cell 已完成“找字”；组合名 `內本/內征·R4` 只作 parser fallback。

| id | ox | role | 推荐字词 | 字源状态 | 无歧义读法 | 备选/备注 |
|---|---|---|---|---|---|---|
| R5-ooooo-cell | ooooo | cell | 力 | 實字 alias | 內本·動（力） | canonical fallback `內本·動`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-ooooo-operator | ooooo | operator | 恒 | XOR 算子字组 | 恒等算子 | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-oooox-cell | oooox | cell | 作 | 實字 alias | 內征·動（作） | canonical fallback `內征·動`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-oooox-operator | oooox | operator | 易內 | XOR 算子字组 | 翻y5（易內） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-oooxo-cell | oooxo | cell | 徑 | 實字 alias | 內本·行（徑） | canonical fallback `內本·行`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-oooxo-operator | oooxo | operator | 臨 | XOR 算子字组 | 翻y4（臨） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-oooxx-cell | oooxx | cell | 履 | 實字 alias | 內征·行（履） | canonical fallback `內征·行`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-oooxx-operator | oooxx | operator | 臨易內 | XOR 算子字组 | 翻y4+y5（臨+易內） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-ooxoo-cell | ooxoo | cell | 質 | 實字 alias | 內本·化（質） | canonical fallback `內本·化`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-ooxoo-operator | ooxoo | operator | 變 | XOR 算子字组 | 翻y3（變） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-ooxox-cell | ooxox | cell | 革 | 實字 alias | 內征·化（革） | canonical fallback `內征·化`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-ooxox-operator | ooxox | operator | 變易內 | XOR 算子字组 | 翻y3+y5（變+易內） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-ooxxo-cell | ooxxo | cell | 源 | 實字 alias | 內本·流（源） | canonical fallback `內本·流`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-ooxxo-operator | ooxxo | operator | 變臨 | XOR 算子字组 | 翻y3+y4（變+臨） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-ooxxx-cell | ooxxx | cell | 衍 | 實字 alias | 內征·流（衍） | canonical fallback `內征·流`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-ooxxx-operator | ooxxx | operator | 變臨易內 | XOR 算子字组 | 翻y3+y4+y5（變+臨+易內） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-oxooo-cell | oxooo | cell | 芽 | 實字 alias | 內本·萌（芽） | canonical fallback `內本·萌`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-oxooo-operator | oxooo | operator | 化 | XOR 算子字组 | 翻y2（化） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-oxoox-cell | oxoox | cell | 茁 | 實字 alias | 內征·萌（茁） | canonical fallback `內征·萌`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-oxoox-operator | oxoox | operator | 化易內 | XOR 算子字组 | 翻y2+y5（化+易內） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-oxoxo-cell | oxoxo | cell | 根 | 實字 alias | 內本·長（根） | canonical fallback `內本·長`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-oxoxo-operator | oxoxo | operator | 化臨 | XOR 算子字组 | 翻y2+y4（化+臨） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-oxoxx-cell | oxoxx | cell | 滋 | 實字 alias | 內征·長（滋） | canonical fallback `內征·長`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-oxoxx-operator | oxoxx | operator | 化臨易內 | XOR 算子字组 | 翻y2+y4+y5（化+臨+易內） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-oxxoo-cell | oxxoo | cell | 蓄 | 實字 alias | 內本·發（蓄） | canonical fallback `內本·發`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-oxxoo-operator | oxxoo | operator | 化變 | XOR 算子字组 | 翻y2+y3（化+變） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-oxxox-cell | oxxox | cell | 啟 | 實字 alias | 內征·發（啟） | canonical fallback `內征·發`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-oxxox-operator | oxxox | operator | 化變易內 | XOR 算子字组 | 翻y2+y3+y5（化+變+易內） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-oxxxo-cell | oxxxo | cell | 緒 | 實字 alias | 內本·續（緒） | canonical fallback `內本·續`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-oxxxo-operator | oxxxo | operator | 化變臨 | XOR 算子字组 | 翻y2+y3+y4（化+變+臨） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-oxxxx-cell | oxxxx | cell | 繼 | 實字 alias | 內征·續（繼） | canonical fallback `內征·續`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-oxxxx-operator | oxxxx | operator | 化變臨易內 | XOR 算子字组 | 翻y2+y3+y4+y5（化+變+臨+易內） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-xoooo-cell | xoooo | cell | 由 | 實字 alias | 內本·緣（由） | canonical fallback `內本·緣`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-xoooo-operator | xoooo | operator | 改 | XOR 算子字组 | 翻y1（改） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-xooox-cell | xooox | cell | 牽 | 實字 alias | 內征·緣（牽） | canonical fallback `內征·緣`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-xooox-operator | xooox | operator | 改易內 | XOR 算子字组 | 翻y1+y5（改+易內） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-xooxo-cell | xooxo | cell | 渠 | 實字 alias | 內本·通（渠） | canonical fallback `內本·通`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-xooxo-operator | xooxo | operator | 改臨 | XOR 算子字组 | 翻y1+y4（改+臨） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-xooxx-cell | xooxx | cell | 達 | 實字 alias | 內征·通（達） | canonical fallback `內征·通`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-xooxx-operator | xooxx | operator | 改臨易內 | XOR 算子字组 | 翻y1+y4+y5（改+臨+易內） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-xoxoo-cell | xoxoo | cell | 契 | 實字 alias | 內本·會（契） | canonical fallback `內本·會`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-xoxoo-operator | xoxoo | operator | 改變 | XOR 算子字组 | 翻y1+y3（改+變） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-xoxox-cell | xoxox | cell | 合 | 實字 alias | 內征·會（合） | canonical fallback `內征·會`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-xoxox-operator | xoxox | operator | 改變易內 | XOR 算子字组 | 翻y1+y3+y5（改+變+易內） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-xoxxo-cell | xoxxo | cell | 綱 | 實字 alias | 內本·系（綱） | canonical fallback `內本·系`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-xoxxo-operator | xoxxo | operator | 改變臨 | XOR 算子字组 | 翻y1+y3+y4（改+變+臨） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-xoxxx-cell | xoxxx | cell | 維 | 實字 alias | 內征·系（維） | canonical fallback `內征·系`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-xoxxx-operator | xoxxx | operator | 改變臨易內 | XOR 算子字组 | 翻y1+y3+y4+y5（改+變+臨+易內） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-xxooo-cell | xxooo | cell | 象 | 實字 alias | 內本·兆（象） | canonical fallback `內本·兆`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-xxooo-operator | xxooo | operator | 改化 | XOR 算子字组 | 翻y1+y2（改+化） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-xxoox-cell | xxoox | cell | 占 | 實字 alias | 內征·兆（占） | canonical fallback `內征·兆`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-xxoox-operator | xxoox | operator | 改化易內 | XOR 算子字组 | 翻y1+y2+y5（改+化+易內） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-xxoxo-cell | xxoxo | cell | 向 | 實字 alias | 內本·趨（向） | canonical fallback `內本·趨`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-xxoxo-operator | xxoxo | operator | 改化臨 | XOR 算子字组 | 翻y1+y2+y4（改+化+臨） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-xxoxx-cell | xxoxx | cell | 赴 | 實字 alias | 內征·趨（赴） | canonical fallback `內征·趨`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-xxoxx-operator | xxoxx | operator | 改化臨易內 | XOR 算子字组 | 翻y1+y2+y4+y5（改+化+臨+易內） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-xxxoo-cell | xxxoo | cell | 故 | 實字 alias | 內本·變（故） | canonical fallback `內本·變`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-xxxoo-operator | xxxoo | operator | 改化變 | XOR 算子字组 | 翻y1+y2+y3（改+化+變） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-xxxox-cell | xxxox | cell | 更 | 實字 alias | 內征·變（更） | canonical fallback `內征·變`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-xxxox-operator | xxxox | operator | 改化變易內 | XOR 算子字组 | 翻y1+y2+y3+y5（改+化+變+易內） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-xxxxo-cell | xxxxo | cell | 記 | 實字 alias | 內本·史（記） | canonical fallback `內本·史`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-xxxxo-operator | xxxxo | operator | 改化變臨 | XOR 算子字组 | 翻y1+y2+y3+y4（改+化+變+臨） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
| R5-xxxxx-cell | xxxxx | cell | 述 | 實字 alias | 內征·史（述） | canonical fallback `內征·史`；Lean carrier remains `Wuyao = Mian × Bool` |
| R5-xxxxx-operator | xxxxx | operator | 改化變臨易內 | XOR 算子字组 | 翻y1+y2+y3+y4+y5（改+化+變+臨+易內） | 五位 mask 由 `改/化/變/臨/易內` 逐位组合；零 mask 为 `恒`。 |
