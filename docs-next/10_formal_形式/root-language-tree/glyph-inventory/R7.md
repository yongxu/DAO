# R7 因 — 256 interface glyph inventory

> 状态：operator review v0.3 (2026-05-13)。
> 口径：逐行列出 R7 的 128 个 cell 字词与 128 个 operator 字组。
> 判断：`素/跡` 不再推荐；默认改为当代中文可直读的 `無跡/有跡`；operator 按 `變化/改變` 收束可读组合。

| id | ox | role | 推荐字词 | 字源状态 | 无歧义读法 | 备选/备注 |
|---|---|---|---|---|---|---|
| R7-ooooooo-cell | ooooooo | cell | 無跡·乾 | 透明状态组合 | 無跡·本本·動（1 乾） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooooooo-operator | ooooooo | operator | 恒 | XOR 算子字组 | 恒等算子 | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oooooox-cell | oooooox | cell | 有跡·乾 | 透明状态组合 | 有跡·本本·動（1 乾） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oooooox-operator | oooooox | operator | 印 | XOR 算子字组 | 翻y7（印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oooooxo-cell | oooooxo | cell | 無跡·夬 | 透明状态组合 | 無跡·本征·動（43 夬） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oooooxo-operator | oooooxo | operator | 易外 | XOR 算子字组 | 翻y6（易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oooooxx-cell | oooooxx | cell | 有跡·夬 | 透明状态组合 | 有跡·本征·動（43 夬） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oooooxx-operator | oooooxx | operator | 易外+印 | XOR 算子字组 | 翻y6+y7（易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooooxoo-cell | ooooxoo | cell | 無跡·大有 | 透明状态组合 | 無跡·征本·動（14 大有） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooooxoo-operator | ooooxoo | operator | 易內 | XOR 算子字组 | 翻y5（易內） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooooxox-cell | ooooxox | cell | 有跡·大有 | 透明状态组合 | 有跡·征本·動（14 大有） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooooxox-operator | ooooxox | operator | 易內+印 | XOR 算子字组 | 翻y5+y7（易內+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooooxxo-cell | ooooxxo | cell | 無跡·大壮 | 透明状态组合 | 無跡·征征·動（34 大壮） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooooxxo-operator | ooooxxo | operator | 易內+易外 | XOR 算子字组 | 翻y5+y6（易內+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooooxxx-cell | ooooxxx | cell | 有跡·大壮 | 透明状态组合 | 有跡·征征·動（34 大壮） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooooxxx-operator | ooooxxx | operator | 易內+易外+印 | XOR 算子字组 | 翻y5+y6+y7（易內+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oooxooo-cell | oooxooo | cell | 無跡·小畜 | 透明状态组合 | 無跡·本本·行（9 小畜） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oooxooo-operator | oooxooo | operator | 臨 | XOR 算子字组 | 翻y4（臨） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oooxoox-cell | oooxoox | cell | 有跡·小畜 | 透明状态组合 | 有跡·本本·行（9 小畜） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oooxoox-operator | oooxoox | operator | 臨+印 | XOR 算子字组 | 翻y4+y7（臨+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oooxoxo-cell | oooxoxo | cell | 無跡·需 | 透明状态组合 | 無跡·本征·行（5 需） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oooxoxo-operator | oooxoxo | operator | 臨+易外 | XOR 算子字组 | 翻y4+y6（臨+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oooxoxx-cell | oooxoxx | cell | 有跡·需 | 透明状态组合 | 有跡·本征·行（5 需） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oooxoxx-operator | oooxoxx | operator | 臨+易外+印 | XOR 算子字组 | 翻y4+y6+y7（臨+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oooxxoo-cell | oooxxoo | cell | 無跡·大畜 | 透明状态组合 | 無跡·征本·行（26 大畜） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oooxxoo-operator | oooxxoo | operator | 臨+易內 | XOR 算子字组 | 翻y4+y5（臨+易內） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oooxxox-cell | oooxxox | cell | 有跡·大畜 | 透明状态组合 | 有跡·征本·行（26 大畜） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oooxxox-operator | oooxxox | operator | 臨+易內+印 | XOR 算子字组 | 翻y4+y5+y7（臨+易內+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oooxxxo-cell | oooxxxo | cell | 無跡·泰 | 透明状态组合 | 無跡·征征·行（11 泰） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oooxxxo-operator | oooxxxo | operator | 臨+易內+易外 | XOR 算子字组 | 翻y4+y5+y6（臨+易內+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oooxxxx-cell | oooxxxx | cell | 有跡·泰 | 透明状态组合 | 有跡·征征·行（11 泰） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oooxxxx-operator | oooxxxx | operator | 臨+易內+易外+印 | XOR 算子字组 | 翻y4+y5+y6+y7（臨+易內+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooxoooo-cell | ooxoooo | cell | 無跡·履 | 透明状态组合 | 無跡·本本·化（10 履） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooxoooo-operator | ooxoooo | operator | 變 | XOR 算子字组 | 翻y3（變） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooxooox-cell | ooxooox | cell | 有跡·履 | 透明状态组合 | 有跡·本本·化（10 履） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooxooox-operator | ooxooox | operator | 變+印 | XOR 算子字组 | 翻y3+y7（變+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooxooxo-cell | ooxooxo | cell | 無跡·兑 | 透明状态组合 | 無跡·本征·化（58 兑） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooxooxo-operator | ooxooxo | operator | 變+易外 | XOR 算子字组 | 翻y3+y6（變+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooxooxx-cell | ooxooxx | cell | 有跡·兑 | 透明状态组合 | 有跡·本征·化（58 兑） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooxooxx-operator | ooxooxx | operator | 變+易外+印 | XOR 算子字组 | 翻y3+y6+y7（變+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooxoxoo-cell | ooxoxoo | cell | 無跡·睽 | 透明状态组合 | 無跡·征本·化（38 睽） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooxoxoo-operator | ooxoxoo | operator | 變+易內 | XOR 算子字组 | 翻y3+y5（變+易內） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooxoxox-cell | ooxoxox | cell | 有跡·睽 | 透明状态组合 | 有跡·征本·化（38 睽） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooxoxox-operator | ooxoxox | operator | 變+易內+印 | XOR 算子字组 | 翻y3+y5+y7（變+易內+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooxoxxo-cell | ooxoxxo | cell | 無跡·归妹 | 透明状态组合 | 無跡·征征·化（54 归妹） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooxoxxo-operator | ooxoxxo | operator | 變+易內+易外 | XOR 算子字组 | 翻y3+y5+y6（變+易內+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooxoxxx-cell | ooxoxxx | cell | 有跡·归妹 | 透明状态组合 | 有跡·征征·化（54 归妹） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooxoxxx-operator | ooxoxxx | operator | 變+易內+易外+印 | XOR 算子字组 | 翻y3+y5+y6+y7（變+易內+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooxxooo-cell | ooxxooo | cell | 無跡·中孚 | 透明状态组合 | 無跡·本本·流（61 中孚） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooxxooo-operator | ooxxooo | operator | 變+臨 | XOR 算子字组 | 翻y3+y4（變+臨） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooxxoox-cell | ooxxoox | cell | 有跡·中孚 | 透明状态组合 | 有跡·本本·流（61 中孚） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooxxoox-operator | ooxxoox | operator | 變+臨+印 | XOR 算子字组 | 翻y3+y4+y7（變+臨+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooxxoxo-cell | ooxxoxo | cell | 無跡·节 | 透明状态组合 | 無跡·本征·流（60 节） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooxxoxo-operator | ooxxoxo | operator | 變+臨+易外 | XOR 算子字组 | 翻y3+y4+y6（變+臨+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooxxoxx-cell | ooxxoxx | cell | 有跡·节 | 透明状态组合 | 有跡·本征·流（60 节） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooxxoxx-operator | ooxxoxx | operator | 變+臨+易外+印 | XOR 算子字组 | 翻y3+y4+y6+y7（變+臨+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooxxxoo-cell | ooxxxoo | cell | 無跡·损 | 透明状态组合 | 無跡·征本·流（41 损） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooxxxoo-operator | ooxxxoo | operator | 變+臨+易內 | XOR 算子字组 | 翻y3+y4+y5（變+臨+易內） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooxxxox-cell | ooxxxox | cell | 有跡·损 | 透明状态组合 | 有跡·征本·流（41 损） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooxxxox-operator | ooxxxox | operator | 變+臨+易內+印 | XOR 算子字组 | 翻y3+y4+y5+y7（變+臨+易內+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooxxxxo-cell | ooxxxxo | cell | 無跡·临 | 透明状态组合 | 無跡·征征·流（19 临） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooxxxxo-operator | ooxxxxo | operator | 變+臨+易內+易外 | XOR 算子字组 | 翻y3+y4+y5+y6（變+臨+易內+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-ooxxxxx-cell | ooxxxxx | cell | 有跡·临 | 透明状态组合 | 有跡·征征·流（19 临） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-ooxxxxx-operator | ooxxxxx | operator | 變+臨+易內+易外+印 | XOR 算子字组 | 翻y3+y4+y5+y6+y7（變+臨+易內+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxooooo-cell | oxooooo | cell | 無跡·同人 | 透明状态组合 | 無跡·本本·萌（13 同人） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxooooo-operator | oxooooo | operator | 化 | XOR 算子字组 | 翻y2（化） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxoooox-cell | oxoooox | cell | 有跡·同人 | 透明状态组合 | 有跡·本本·萌（13 同人） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxoooox-operator | oxoooox | operator | 化+印 | XOR 算子字组 | 翻y2+y7（化+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxoooxo-cell | oxoooxo | cell | 無跡·革 | 透明状态组合 | 無跡·本征·萌（49 革） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxoooxo-operator | oxoooxo | operator | 化+易外 | XOR 算子字组 | 翻y2+y6（化+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxoooxx-cell | oxoooxx | cell | 有跡·革 | 透明状态组合 | 有跡·本征·萌（49 革） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxoooxx-operator | oxoooxx | operator | 化+易外+印 | XOR 算子字组 | 翻y2+y6+y7（化+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxooxoo-cell | oxooxoo | cell | 無跡·离 | 透明状态组合 | 無跡·征本·萌（30 离） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxooxoo-operator | oxooxoo | operator | 化+易內 | XOR 算子字组 | 翻y2+y5（化+易內） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxooxox-cell | oxooxox | cell | 有跡·离 | 透明状态组合 | 有跡·征本·萌（30 离） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxooxox-operator | oxooxox | operator | 化+易內+印 | XOR 算子字组 | 翻y2+y5+y7（化+易內+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxooxxo-cell | oxooxxo | cell | 無跡·丰 | 透明状态组合 | 無跡·征征·萌（55 丰） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxooxxo-operator | oxooxxo | operator | 化+易內+易外 | XOR 算子字组 | 翻y2+y5+y6（化+易內+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxooxxx-cell | oxooxxx | cell | 有跡·丰 | 透明状态组合 | 有跡·征征·萌（55 丰） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxooxxx-operator | oxooxxx | operator | 化+易內+易外+印 | XOR 算子字组 | 翻y2+y5+y6+y7（化+易內+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxoxooo-cell | oxoxooo | cell | 無跡·家人 | 透明状态组合 | 無跡·本本·長（37 家人） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxoxooo-operator | oxoxooo | operator | 化+臨 | XOR 算子字组 | 翻y2+y4（化+臨） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxoxoox-cell | oxoxoox | cell | 有跡·家人 | 透明状态组合 | 有跡·本本·長（37 家人） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxoxoox-operator | oxoxoox | operator | 化+臨+印 | XOR 算子字组 | 翻y2+y4+y7（化+臨+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxoxoxo-cell | oxoxoxo | cell | 無跡·既济 | 透明状态组合 | 無跡·本征·長（63 既济） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxoxoxo-operator | oxoxoxo | operator | 化+臨+易外 | XOR 算子字组 | 翻y2+y4+y6（化+臨+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxoxoxx-cell | oxoxoxx | cell | 有跡·既济 | 透明状态组合 | 有跡·本征·長（63 既济） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxoxoxx-operator | oxoxoxx | operator | 化+臨+易外+印 | XOR 算子字组 | 翻y2+y4+y6+y7（化+臨+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxoxxoo-cell | oxoxxoo | cell | 無跡·贲 | 透明状态组合 | 無跡·征本·長（22 贲） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxoxxoo-operator | oxoxxoo | operator | 化+臨+易內 | XOR 算子字组 | 翻y2+y4+y5（化+臨+易內） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxoxxox-cell | oxoxxox | cell | 有跡·贲 | 透明状态组合 | 有跡·征本·長（22 贲） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxoxxox-operator | oxoxxox | operator | 化+臨+易內+印 | XOR 算子字组 | 翻y2+y4+y5+y7（化+臨+易內+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxoxxxo-cell | oxoxxxo | cell | 無跡·明夷 | 透明状态组合 | 無跡·征征·長（36 明夷） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxoxxxo-operator | oxoxxxo | operator | 化+臨+易內+易外 | XOR 算子字组 | 翻y2+y4+y5+y6（化+臨+易內+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxoxxxx-cell | oxoxxxx | cell | 有跡·明夷 | 透明状态组合 | 有跡·征征·長（36 明夷） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxoxxxx-operator | oxoxxxx | operator | 化+臨+易內+易外+印 | XOR 算子字组 | 翻y2+y4+y5+y6+y7（化+臨+易內+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-oxxoooo-cell | oxxoooo | cell | 無跡·无妄 | 透明状态组合 | 無跡·本本·發（25 无妄） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxxoooo-operator | oxxoooo | operator | 變化 | XOR 算子字组 | 翻y2+y3（變化） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-oxxooox-cell | oxxooox | cell | 有跡·无妄 | 透明状态组合 | 有跡·本本·發（25 无妄） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxxooox-operator | oxxooox | operator | 變化+印 | XOR 算子字组 | 翻y2+y3+y7（變化+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-oxxooxo-cell | oxxooxo | cell | 無跡·随 | 透明状态组合 | 無跡·本征·發（17 随） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxxooxo-operator | oxxooxo | operator | 變化+易外 | XOR 算子字组 | 翻y2+y3+y6（變化+易外） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-oxxooxx-cell | oxxooxx | cell | 有跡·随 | 透明状态组合 | 有跡·本征·發（17 随） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxxooxx-operator | oxxooxx | operator | 變化+易外+印 | XOR 算子字组 | 翻y2+y3+y6+y7（變化+易外+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-oxxoxoo-cell | oxxoxoo | cell | 無跡·噬嗑 | 透明状态组合 | 無跡·征本·發（21 噬嗑） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxxoxoo-operator | oxxoxoo | operator | 變化+易內 | XOR 算子字组 | 翻y2+y3+y5（變化+易內） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-oxxoxox-cell | oxxoxox | cell | 有跡·噬嗑 | 透明状态组合 | 有跡·征本·發（21 噬嗑） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxxoxox-operator | oxxoxox | operator | 變化+易內+印 | XOR 算子字组 | 翻y2+y3+y5+y7（變化+易內+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-oxxoxxo-cell | oxxoxxo | cell | 無跡·震 | 透明状态组合 | 無跡·征征·發（51 震） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxxoxxo-operator | oxxoxxo | operator | 變化+易內+易外 | XOR 算子字组 | 翻y2+y3+y5+y6（變化+易內+易外） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-oxxoxxx-cell | oxxoxxx | cell | 有跡·震 | 透明状态组合 | 有跡·征征·發（51 震） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxxoxxx-operator | oxxoxxx | operator | 變化+易內+易外+印 | XOR 算子字组 | 翻y2+y3+y5+y6+y7（變化+易內+易外+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-oxxxooo-cell | oxxxooo | cell | 無跡·益 | 透明状态组合 | 無跡·本本·續（42 益） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxxxooo-operator | oxxxooo | operator | 變化+臨 | XOR 算子字组 | 翻y2+y3+y4（變化+臨） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-oxxxoox-cell | oxxxoox | cell | 有跡·益 | 透明状态组合 | 有跡·本本·續（42 益） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxxxoox-operator | oxxxoox | operator | 變化+臨+印 | XOR 算子字组 | 翻y2+y3+y4+y7（變化+臨+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-oxxxoxo-cell | oxxxoxo | cell | 無跡·屯 | 透明状态组合 | 無跡·本征·續（3 屯） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxxxoxo-operator | oxxxoxo | operator | 變化+臨+易外 | XOR 算子字组 | 翻y2+y3+y4+y6（變化+臨+易外） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-oxxxoxx-cell | oxxxoxx | cell | 有跡·屯 | 透明状态组合 | 有跡·本征·續（3 屯） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxxxoxx-operator | oxxxoxx | operator | 變化+臨+易外+印 | XOR 算子字组 | 翻y2+y3+y4+y6+y7（變化+臨+易外+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-oxxxxoo-cell | oxxxxoo | cell | 無跡·颐 | 透明状态组合 | 無跡·征本·續（27 颐） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxxxxoo-operator | oxxxxoo | operator | 變化+臨+易內 | XOR 算子字组 | 翻y2+y3+y4+y5（變化+臨+易內） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-oxxxxox-cell | oxxxxox | cell | 有跡·颐 | 透明状态组合 | 有跡·征本·續（27 颐） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxxxxox-operator | oxxxxox | operator | 變化+臨+易內+印 | XOR 算子字组 | 翻y2+y3+y4+y5+y7（變化+臨+易內+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-oxxxxxo-cell | oxxxxxo | cell | 無跡·复 | 透明状态组合 | 無跡·征征·續（24 复） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxxxxxo-operator | oxxxxxo | operator | 變化+臨+易內+易外 | XOR 算子字组 | 翻y2+y3+y4+y5+y6（變化+臨+易內+易外） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-oxxxxxx-cell | oxxxxxx | cell | 有跡·复 | 透明状态组合 | 有跡·征征·續（24 复） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-oxxxxxx-operator | oxxxxxx | operator | 變化+臨+易內+易外+印 | XOR 算子字组 | 翻y2+y3+y4+y5+y6+y7（變化+臨+易內+易外+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xoooooo-cell | xoooooo | cell | 無跡·姤 | 透明状态组合 | 無跡·本本·緣（44 姤） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoooooo-operator | xoooooo | operator | 改 | XOR 算子字组 | 翻y1（改） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xooooox-cell | xooooox | cell | 有跡·姤 | 透明状态组合 | 有跡·本本·緣（44 姤） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xooooox-operator | xooooox | operator | 改+印 | XOR 算子字组 | 翻y1+y7（改+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xooooxo-cell | xooooxo | cell | 無跡·大过 | 透明状态组合 | 無跡·本征·緣（28 大过） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xooooxo-operator | xooooxo | operator | 改+易外 | XOR 算子字组 | 翻y1+y6（改+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xooooxx-cell | xooooxx | cell | 有跡·大过 | 透明状态组合 | 有跡·本征·緣（28 大过） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xooooxx-operator | xooooxx | operator | 改+易外+印 | XOR 算子字组 | 翻y1+y6+y7（改+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xoooxoo-cell | xoooxoo | cell | 無跡·鼎 | 透明状态组合 | 無跡·征本·緣（50 鼎） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoooxoo-operator | xoooxoo | operator | 改+易內 | XOR 算子字组 | 翻y1+y5（改+易內） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xoooxox-cell | xoooxox | cell | 有跡·鼎 | 透明状态组合 | 有跡·征本·緣（50 鼎） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoooxox-operator | xoooxox | operator | 改+易內+印 | XOR 算子字组 | 翻y1+y5+y7（改+易內+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xoooxxo-cell | xoooxxo | cell | 無跡·恒 | 透明状态组合 | 無跡·征征·緣（32 恒） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoooxxo-operator | xoooxxo | operator | 改+易內+易外 | XOR 算子字组 | 翻y1+y5+y6（改+易內+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xoooxxx-cell | xoooxxx | cell | 有跡·恒 | 透明状态组合 | 有跡·征征·緣（32 恒） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoooxxx-operator | xoooxxx | operator | 改+易內+易外+印 | XOR 算子字组 | 翻y1+y5+y6+y7（改+易內+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xooxooo-cell | xooxooo | cell | 無跡·巽 | 透明状态组合 | 無跡·本本·通（57 巽） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xooxooo-operator | xooxooo | operator | 改+臨 | XOR 算子字组 | 翻y1+y4（改+臨） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xooxoox-cell | xooxoox | cell | 有跡·巽 | 透明状态组合 | 有跡·本本·通（57 巽） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xooxoox-operator | xooxoox | operator | 改+臨+印 | XOR 算子字组 | 翻y1+y4+y7（改+臨+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xooxoxo-cell | xooxoxo | cell | 無跡·井 | 透明状态组合 | 無跡·本征·通（48 井） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xooxoxo-operator | xooxoxo | operator | 改+臨+易外 | XOR 算子字组 | 翻y1+y4+y6（改+臨+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xooxoxx-cell | xooxoxx | cell | 有跡·井 | 透明状态组合 | 有跡·本征·通（48 井） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xooxoxx-operator | xooxoxx | operator | 改+臨+易外+印 | XOR 算子字组 | 翻y1+y4+y6+y7（改+臨+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xooxxoo-cell | xooxxoo | cell | 無跡·蛊 | 透明状态组合 | 無跡·征本·通（18 蛊） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xooxxoo-operator | xooxxoo | operator | 改+臨+易內 | XOR 算子字组 | 翻y1+y4+y5（改+臨+易內） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xooxxox-cell | xooxxox | cell | 有跡·蛊 | 透明状态组合 | 有跡·征本·通（18 蛊） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xooxxox-operator | xooxxox | operator | 改+臨+易內+印 | XOR 算子字组 | 翻y1+y4+y5+y7（改+臨+易內+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xooxxxo-cell | xooxxxo | cell | 無跡·升 | 透明状态组合 | 無跡·征征·通（46 升） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xooxxxo-operator | xooxxxo | operator | 改+臨+易內+易外 | XOR 算子字组 | 翻y1+y4+y5+y6（改+臨+易內+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xooxxxx-cell | xooxxxx | cell | 有跡·升 | 透明状态组合 | 有跡·征征·通（46 升） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xooxxxx-operator | xooxxxx | operator | 改+臨+易內+易外+印 | XOR 算子字组 | 翻y1+y4+y5+y6+y7（改+臨+易內+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xoxoooo-cell | xoxoooo | cell | 無跡·讼 | 透明状态组合 | 無跡·本本·會（6 讼） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoxoooo-operator | xoxoooo | operator | 改變 | XOR 算子字组 | 翻y1+y3（改變） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xoxooox-cell | xoxooox | cell | 有跡·讼 | 透明状态组合 | 有跡·本本·會（6 讼） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoxooox-operator | xoxooox | operator | 改變+印 | XOR 算子字组 | 翻y1+y3+y7（改變+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xoxooxo-cell | xoxooxo | cell | 無跡·困 | 透明状态组合 | 無跡·本征·會（47 困） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoxooxo-operator | xoxooxo | operator | 改變+易外 | XOR 算子字组 | 翻y1+y3+y6（改變+易外） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xoxooxx-cell | xoxooxx | cell | 有跡·困 | 透明状态组合 | 有跡·本征·會（47 困） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoxooxx-operator | xoxooxx | operator | 改變+易外+印 | XOR 算子字组 | 翻y1+y3+y6+y7（改變+易外+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xoxoxoo-cell | xoxoxoo | cell | 無跡·未济 | 透明状态组合 | 無跡·征本·會（64 未济） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoxoxoo-operator | xoxoxoo | operator | 改變+易內 | XOR 算子字组 | 翻y1+y3+y5（改變+易內） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xoxoxox-cell | xoxoxox | cell | 有跡·未济 | 透明状态组合 | 有跡·征本·會（64 未济） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoxoxox-operator | xoxoxox | operator | 改變+易內+印 | XOR 算子字组 | 翻y1+y3+y5+y7（改變+易內+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xoxoxxo-cell | xoxoxxo | cell | 無跡·解 | 透明状态组合 | 無跡·征征·會（40 解） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoxoxxo-operator | xoxoxxo | operator | 改變+易內+易外 | XOR 算子字组 | 翻y1+y3+y5+y6（改變+易內+易外） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xoxoxxx-cell | xoxoxxx | cell | 有跡·解 | 透明状态组合 | 有跡·征征·會（40 解） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoxoxxx-operator | xoxoxxx | operator | 改變+易內+易外+印 | XOR 算子字组 | 翻y1+y3+y5+y6+y7（改變+易內+易外+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xoxxooo-cell | xoxxooo | cell | 無跡·涣 | 透明状态组合 | 無跡·本本·系（59 涣） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoxxooo-operator | xoxxooo | operator | 改變+臨 | XOR 算子字组 | 翻y1+y3+y4（改變+臨） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xoxxoox-cell | xoxxoox | cell | 有跡·涣 | 透明状态组合 | 有跡·本本·系（59 涣） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoxxoox-operator | xoxxoox | operator | 改變+臨+印 | XOR 算子字组 | 翻y1+y3+y4+y7（改變+臨+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xoxxoxo-cell | xoxxoxo | cell | 無跡·坎 | 透明状态组合 | 無跡·本征·系（29 坎） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoxxoxo-operator | xoxxoxo | operator | 改變+臨+易外 | XOR 算子字组 | 翻y1+y3+y4+y6（改變+臨+易外） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xoxxoxx-cell | xoxxoxx | cell | 有跡·坎 | 透明状态组合 | 有跡·本征·系（29 坎） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoxxoxx-operator | xoxxoxx | operator | 改變+臨+易外+印 | XOR 算子字组 | 翻y1+y3+y4+y6+y7（改變+臨+易外+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xoxxxoo-cell | xoxxxoo | cell | 無跡·蒙 | 透明状态组合 | 無跡·征本·系（4 蒙） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoxxxoo-operator | xoxxxoo | operator | 改變+臨+易內 | XOR 算子字组 | 翻y1+y3+y4+y5（改變+臨+易內） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xoxxxox-cell | xoxxxox | cell | 有跡·蒙 | 透明状态组合 | 有跡·征本·系（4 蒙） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoxxxox-operator | xoxxxox | operator | 改變+臨+易內+印 | XOR 算子字组 | 翻y1+y3+y4+y5+y7（改變+臨+易內+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xoxxxxo-cell | xoxxxxo | cell | 無跡·师 | 透明状态组合 | 無跡·征征·系（7 师） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoxxxxo-operator | xoxxxxo | operator | 改變+臨+易內+易外 | XOR 算子字组 | 翻y1+y3+y4+y5+y6（改變+臨+易內+易外） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xoxxxxx-cell | xoxxxxx | cell | 有跡·师 | 透明状态组合 | 有跡·征征·系（7 师） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xoxxxxx-operator | xoxxxxx | operator | 改變+臨+易內+易外+印 | XOR 算子字组 | 翻y1+y3+y4+y5+y6+y7（改變+臨+易內+易外+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xxooooo-cell | xxooooo | cell | 無跡·遁 | 透明状态组合 | 無跡·本本·兆（33 遁） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxooooo-operator | xxooooo | operator | 改+化 | XOR 算子字组 | 翻y1+y2（改+化） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xxoooox-cell | xxoooox | cell | 有跡·遁 | 透明状态组合 | 有跡·本本·兆（33 遁） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxoooox-operator | xxoooox | operator | 改+化+印 | XOR 算子字组 | 翻y1+y2+y7（改+化+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xxoooxo-cell | xxoooxo | cell | 無跡·咸 | 透明状态组合 | 無跡·本征·兆（31 咸） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxoooxo-operator | xxoooxo | operator | 改+化+易外 | XOR 算子字组 | 翻y1+y2+y6（改+化+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xxoooxx-cell | xxoooxx | cell | 有跡·咸 | 透明状态组合 | 有跡·本征·兆（31 咸） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxoooxx-operator | xxoooxx | operator | 改+化+易外+印 | XOR 算子字组 | 翻y1+y2+y6+y7（改+化+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xxooxoo-cell | xxooxoo | cell | 無跡·旅 | 透明状态组合 | 無跡·征本·兆（56 旅） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxooxoo-operator | xxooxoo | operator | 改+化+易內 | XOR 算子字组 | 翻y1+y2+y5（改+化+易內） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xxooxox-cell | xxooxox | cell | 有跡·旅 | 透明状态组合 | 有跡·征本·兆（56 旅） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxooxox-operator | xxooxox | operator | 改+化+易內+印 | XOR 算子字组 | 翻y1+y2+y5+y7（改+化+易內+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xxooxxo-cell | xxooxxo | cell | 無跡·小过 | 透明状态组合 | 無跡·征征·兆（62 小过） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxooxxo-operator | xxooxxo | operator | 改+化+易內+易外 | XOR 算子字组 | 翻y1+y2+y5+y6（改+化+易內+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xxooxxx-cell | xxooxxx | cell | 有跡·小过 | 透明状态组合 | 有跡·征征·兆（62 小过） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxooxxx-operator | xxooxxx | operator | 改+化+易內+易外+印 | XOR 算子字组 | 翻y1+y2+y5+y6+y7（改+化+易內+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xxoxooo-cell | xxoxooo | cell | 無跡·渐 | 透明状态组合 | 無跡·本本·趨（53 渐） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxoxooo-operator | xxoxooo | operator | 改+化+臨 | XOR 算子字组 | 翻y1+y2+y4（改+化+臨） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xxoxoox-cell | xxoxoox | cell | 有跡·渐 | 透明状态组合 | 有跡·本本·趨（53 渐） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxoxoox-operator | xxoxoox | operator | 改+化+臨+印 | XOR 算子字组 | 翻y1+y2+y4+y7（改+化+臨+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xxoxoxo-cell | xxoxoxo | cell | 無跡·蹇 | 透明状态组合 | 無跡·本征·趨（39 蹇） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxoxoxo-operator | xxoxoxo | operator | 改+化+臨+易外 | XOR 算子字组 | 翻y1+y2+y4+y6（改+化+臨+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xxoxoxx-cell | xxoxoxx | cell | 有跡·蹇 | 透明状态组合 | 有跡·本征·趨（39 蹇） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxoxoxx-operator | xxoxoxx | operator | 改+化+臨+易外+印 | XOR 算子字组 | 翻y1+y2+y4+y6+y7（改+化+臨+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xxoxxoo-cell | xxoxxoo | cell | 無跡·艮 | 透明状态组合 | 無跡·征本·趨（52 艮） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxoxxoo-operator | xxoxxoo | operator | 改+化+臨+易內 | XOR 算子字组 | 翻y1+y2+y4+y5（改+化+臨+易內） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xxoxxox-cell | xxoxxox | cell | 有跡·艮 | 透明状态组合 | 有跡·征本·趨（52 艮） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxoxxox-operator | xxoxxox | operator | 改+化+臨+易內+印 | XOR 算子字组 | 翻y1+y2+y4+y5+y7（改+化+臨+易內+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xxoxxxo-cell | xxoxxxo | cell | 無跡·谦 | 透明状态组合 | 無跡·征征·趨（15 谦） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxoxxxo-operator | xxoxxxo | operator | 改+化+臨+易內+易外 | XOR 算子字组 | 翻y1+y2+y4+y5+y6（改+化+臨+易內+易外） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xxoxxxx-cell | xxoxxxx | cell | 有跡·谦 | 透明状态组合 | 有跡·征征·趨（15 谦） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxoxxxx-operator | xxoxxxx | operator | 改+化+臨+易內+易外+印 | XOR 算子字组 | 翻y1+y2+y4+y5+y6+y7（改+化+臨+易內+易外+印） | 七位 mask 由 `改/化/變/臨/易內/易外/印` 逐位组合；多原子用 `+` 分隔，零 mask 为 `恒`。 |
| R7-xxxoooo-cell | xxxoooo | cell | 無跡·否 | 透明状态组合 | 無跡·本本·變（12 否） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxxoooo-operator | xxxoooo | operator | 改+變化 | XOR 算子字组 | 翻y1+y2+y3（改+變化） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xxxooox-cell | xxxooox | cell | 有跡·否 | 透明状态组合 | 有跡·本本·變（12 否） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxxooox-operator | xxxooox | operator | 改+變化+印 | XOR 算子字组 | 翻y1+y2+y3+y7（改+變化+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xxxooxo-cell | xxxooxo | cell | 無跡·萃 | 透明状态组合 | 無跡·本征·變（45 萃） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxxooxo-operator | xxxooxo | operator | 改+變化+易外 | XOR 算子字组 | 翻y1+y2+y3+y6（改+變化+易外） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xxxooxx-cell | xxxooxx | cell | 有跡·萃 | 透明状态组合 | 有跡·本征·變（45 萃） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxxooxx-operator | xxxooxx | operator | 改+變化+易外+印 | XOR 算子字组 | 翻y1+y2+y3+y6+y7（改+變化+易外+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xxxoxoo-cell | xxxoxoo | cell | 無跡·晋 | 透明状态组合 | 無跡·征本·變（35 晋） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxxoxoo-operator | xxxoxoo | operator | 改+變化+易內 | XOR 算子字组 | 翻y1+y2+y3+y5（改+變化+易內） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xxxoxox-cell | xxxoxox | cell | 有跡·晋 | 透明状态组合 | 有跡·征本·變（35 晋） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxxoxox-operator | xxxoxox | operator | 改+變化+易內+印 | XOR 算子字组 | 翻y1+y2+y3+y5+y7（改+變化+易內+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xxxoxxo-cell | xxxoxxo | cell | 無跡·豫 | 透明状态组合 | 無跡·征征·變（16 豫） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxxoxxo-operator | xxxoxxo | operator | 改+變化+易內+易外 | XOR 算子字组 | 翻y1+y2+y3+y5+y6（改+變化+易內+易外） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xxxoxxx-cell | xxxoxxx | cell | 有跡·豫 | 透明状态组合 | 有跡·征征·變（16 豫） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxxoxxx-operator | xxxoxxx | operator | 改+變化+易內+易外+印 | XOR 算子字组 | 翻y1+y2+y3+y5+y6+y7（改+變化+易內+易外+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xxxxooo-cell | xxxxooo | cell | 無跡·观 | 透明状态组合 | 無跡·本本·史（20 观） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxxxooo-operator | xxxxooo | operator | 改+變化+臨 | XOR 算子字组 | 翻y1+y2+y3+y4（改+變化+臨） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xxxxoox-cell | xxxxoox | cell | 有跡·观 | 透明状态组合 | 有跡·本本·史（20 观） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxxxoox-operator | xxxxoox | operator | 改+變化+臨+印 | XOR 算子字组 | 翻y1+y2+y3+y4+y7（改+變化+臨+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xxxxoxo-cell | xxxxoxo | cell | 無跡·比 | 透明状态组合 | 無跡·本征·史（8 比） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxxxoxo-operator | xxxxoxo | operator | 改+變化+臨+易外 | XOR 算子字组 | 翻y1+y2+y3+y4+y6（改+變化+臨+易外） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xxxxoxx-cell | xxxxoxx | cell | 有跡·比 | 透明状态组合 | 有跡·本征·史（8 比） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxxxoxx-operator | xxxxoxx | operator | 改+變化+臨+易外+印 | XOR 算子字组 | 翻y1+y2+y3+y4+y6+y7（改+變化+臨+易外+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xxxxxoo-cell | xxxxxoo | cell | 無跡·剥 | 透明状态组合 | 無跡·征本·史（23 剥） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxxxxoo-operator | xxxxxoo | operator | 改+變化+臨+易內 | XOR 算子字组 | 翻y1+y2+y3+y4+y5（改+變化+臨+易內） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xxxxxox-cell | xxxxxox | cell | 有跡·剥 | 透明状态组合 | 有跡·征本·史（23 剥） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxxxxox-operator | xxxxxox | operator | 改+變化+臨+易內+印 | XOR 算子字组 | 翻y1+y2+y3+y4+y5+y7（改+變化+臨+易內+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xxxxxxo-cell | xxxxxxo | cell | 無跡·坤 | 透明状态组合 | 無跡·征征·史（2 坤） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxxxxxo-operator | xxxxxxo | operator | 改+變化+臨+易內+易外 | XOR 算子字组 | 翻y1+y2+y3+y4+y5+y6（改+變化+臨+易內+易外） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
| R7-xxxxxxx-cell | xxxxxxx | cell | 有跡·坤 | 透明状态组合 | 有跡·征征·史（2 坤） | 对应 因位 false/true；`無因/有因` 只作 formal fallback，`素/跡` 降为历史候选。 |
| R7-xxxxxxx-operator | xxxxxxx | operator | 改+變化+臨+易內+易外+印 | XOR 算子字组 | 翻y1+y2+y3+y4+y5+y6+y7（改+變化+臨+易內+易外+印） | R7 operator audit: lexicalized submasks `變化/改變` are grouped; remaining atoms stay explicit. |
