# 核心框架

核心框架回答一个问题：不同文档怎样指向同一生成骨架。

## 四级生成

旧 `义理/A_经典易传.md` 给出经典二分读法：太极、一分为二、再成四象、再成八卦。它适合作为传统入口，但不是唯一实现。

旧 `义理/D_算子代数.md` 是最薄的形式底座：

| 层级 | 形式读法 | 基数 |
|---|---|---|
| 太极 | `Unit` | 1 |
| 两翼 | `Bool` | 2 |
| 四象 | `Bool × Bool` | 4 |
| 八卦 | `Bool × Bool × Bool` | 8 |

由此可把“生”理解为升维构造，把“合”理解为投影或归一，把“反/变/化/动”理解为布尔分量上的翻转。

## 六征体系

旧 `义理/B_六征体系.md` 是项目主用语义层。三对征为：

| 征对 | 两极 | 中态读法 |
|---|---|---|
| 显隐 | 显 / 隐 | 玄 |
| 限许 | 限 / 许 | 可禁可通之前 |
| 实虚 | 实 / 虚 | 几、妙 |

三对征全取二态时形成 `2^3 = 8` 个八卦位。它与 `Bool^3` 形式同构，但三轴语义不同，不应把三爻混作同一类阴阳。

## 实虚史真

旧 `义理/C_实虚史真.md` 与 `六表_实虚史真/` 提供三轴审校读法：实虚、史维、真假。它适合判断一个命题在事实、历程、认识三方面的位置。新版 docs-next 中，六表应作为参考资料，不在义理正文重复展开。

## 完整算子系统

旧 `义理/G_完整算子系统_八卦互通与归一.md` 将横向互通与纵向生成合在一处。阅读时只需把它视作三件事：

- 生成：从一到二、四、八、六十四。
- 互通：八卦内部经动、化、变等算子互相抵达。
- 归一：复杂结构可经投影、收束、归根回到共同底座。

相关旧入口：`义理/A_经典易传.md`、`义理/B_六征体系.md`、`义理/C_实虚史真.md`、`义理/D_算子代数.md`、`义理/G_完整算子系统_八卦互通与归一.md`、`义理/算子别名总表.md`、`义理/道桥接_ProcessAligned_Dao_ShengshengBuxi.md`。

自动生成索引若存在，可对照 `../_generated/` 中的文件索引与术语索引。
